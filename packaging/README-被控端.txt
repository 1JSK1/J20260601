智控被控端
==========

双击“智控被控端.exe”即可启动。

默认地址：
  http://0.0.0.0:7821

默认配对 Token：
  dev-token

建议为每台设备设置独立 Token：
  智控被控端.exe --host 0.0.0.0 --port 7821 --pairing-token 你的Token

添加设备时填写：
  地址：被控电脑的局域网 IP
  端口：7821
  配对 Token：启动参数中设置的 Token

Windows 防火墙首次询问时，需要允许专用网络访问。
